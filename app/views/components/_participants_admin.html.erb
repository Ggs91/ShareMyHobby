<div class="site-section bg-light">
  <div class="container">
    <div class="row mb-5">
      <div class="col-md-7 text-left border-primary">
        <h2 class="font-weight-light text-primary">Participants</h2>
      </div>
    </div>
    <% @event.participants.each do |participant| %>
    <div class="col-4">
      <div class="d-block d-md-flex listing">
        <% if participant.profile_picture.attached? %>
        <%= image_tag(participant.profile_picture, alt: 'profile picture') %>
        <% end %>
        <div class="lh-content">
          <span class="category">
            <%= participant.last_name %>
            <%= participant.first_name %>
          </span>
          <h3>
            <p>
              Age :
              <%= participant.age %>
            </p>
            <p>
              City :
              <%= participant.department.name %>
            </p>
            <p>
              Phone Number :
              <%= participant.phone_number %>
            </p>
            <p>
              Email :
              <%= participant.email %>
            </p>
          </h3>
          <%= link_to  "Profile" , user_path(participant.id), class:"btn btn-primary" %>
        </div>
      </div>
    </div>
    <% end %>
